<h1>Create Application</h1>

<div class="row">
  <div class="col-md-8">
    <%= form_with url: developer_applications_path, model: @application do |form| %>
      <div class="mb-3">
        <%= form.label :name, "Application Name" %>
        <%= form.text_field :name, class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= form.label :redirect_uri, "Redirect URIs" %>
        <%= form.text_area :redirect_uri, class: "form-control" %>
        <div id="redirectUriHelp" class="form-text">
          Separate each Redirect URI your application will use with a newline.
        </div>
      </div>
      <div class="mb-3">
        <label>Authentication Mode</label>
        <div class="form-check">
          <%= form.radio_button :scopes, "character", class: "form-check-input", checked: true %>
          <%= form.label :scopes_character, "Log in with a character", class: "form-check-label" %>
        </div>
        <div class="form-check">
          <%= form.radio_button :scopes, "user", class: "form-check-input" %>
          <%= form.label :scopes_user, "Log in with the user", class: "form-check-label" %>
        </div>
        <div class="form-text">
          Choose carefully! This setting may not be changed without dev intervention.
        </div>
      </div>
      <div class="mb-3">
        <p>
          Other options are available depending on your application's use case. Please contact an XIVAuth developer if
          you require custom scopes (including <code>characters:all</code> or <code>user:email</code>),
          non-confidential apps, or grant flows like Device Code.
        </p>
      </div>
      <%= form.submit "Create App!", class: "btn btn-primary" %>
    <% end %>
  </div>
  <div class="col-md-4">
    <h4>Request Preview</h4>
    <div class="highlight request-preview">
      One day...
    </div>
  </div>
</div>

