<div class="modal-header">
  <h5 class="modal-title">Confirm Character</h5>
</div>
<div class="modal-body">
  <p>
    We found multiple characters named <strong><%= name %></strong> on <strong><%= world %></strong>. Please choose the
    character you wish to register.
  </p>
  <p>
    If none of these are correct, please make sure that your query is correct and that your character is not hidden
    from search results.
  </p>
  <ul class="list-group mb-3">
    <% candidates.each do |c| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <div class="d-flex align-items-center gap-3">
          <% if c[:avatar_url].present? %>
            <%= image_tag c[:avatar_url], alt: c[:name], class: "rounded", style: "width: 48px; height: 48px;" %>
          <% end %>
          <div>
            <div class="fw-bold"><%= c[:name] %></div>
            <small class="text-muted"><%= c[:world] %> [<%= c[:datacenter] %>]</small>
          </div>
        </div>
        <%= button_to "Select", character_registrations_path, method: :post, params: { character_registration: { lodestone_url: c[:lodestone_id], from_search: true } }, class: "btn btn-primary" %>
      </li>
    <% end %>
  </ul>
</div>
<div class="modal-footer">
  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
</div>

