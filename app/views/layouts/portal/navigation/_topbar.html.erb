<nav class="navbar navbar-expand-xl navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand pride-brand" href="/">XIVAuth</a>
    <div class="sidebar-toggle" data-toggle="sidebar" data-active="true">
      <i class="bi bi-list"></i>
    </div>
    <ul class="navbar-nav ms-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
          <img src="<%= current_user.avatar_url %>" alt="" width="36" height="36" class="rounded-circle me-2">
          <span class="d-none d-md-inline">Hi, <%= current_user&.profile&.display_name || 'Guest' %>!</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
          <li>
            <a class="dropdown-item" href="<%= edit_user_path %>">
              <i class="bi bi-person me-2"></i> Profile
            </a>
          </li>
          <li>
            <%= link_to destroy_user_session_path, class: "dropdown-item text-danger", data: { "turbo-method": :delete } do %>
              <i class="bi bi-box-arrow-left me-2"></i> Log Out
            <% end %>
          </li>
        </ul>
      </li>
    </ul>
  </div>
</nav>
