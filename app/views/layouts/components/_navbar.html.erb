<nav class="navbar navbar-expand-md navbar-dark bg-dark">
  <div class="container">
    <%= link_to "XIVAuth", root_path, :class => "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <% unless current_user.nil? %>
          <li class="nav-item">
            <%= link_to "Characters", characters_path, :class => 'nav-link' %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" aria-expanded="false">Developer</a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item disabled">Request Developer Access</a></li>
              <li><a class="dropdown-item">Applications</a></li>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" aria-expanded="false">Admin</a>
            <ul class="dropdown-menu">
              <li><%= link_to "Users", admin_users_path, :class => 'dropdown-item' %></li>
              <li><%= link_to "Characters", admin_characters_path, :class => 'dropdown-item' %></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <% if current_user.nil? %>
          <li class="nav-item">
            <%= link_to "Sign In / Register", new_user_session_path,
                        :class => 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown" aria-expanded="false">Hi, <%= current_user.username %>!</a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "Profile", profile_path, :class => 'dropdown-item' %>
              </li>
              <li><hr class="dropdown-divider" /></li>
              <li>
                <%= link_to "Sign Out", destroy_user_session_path,
                            data: { turbo_method: :delete },
                            :class => 'dropdown-item' %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>