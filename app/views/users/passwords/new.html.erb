<div class="row justify-content-center">
  <div class="col-lg-4">
    <h2>Forgot your password?</h2>

    <p>We get it, passwords are hard to remember. Fortunately, we have a great solution: The Password Reset Form!</p>
    <p>Enter your account's email below and, if you have an account with us, we will dispatch you an e-mail all speedy-like.</p>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, data: { turbo: false }) do |f| %>
      <%= render "users/shared/error_messages", resource: resource %>
      <%= flash[:recaptcha_error] %>

      <div class="form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>

      <div class="form-group d-grid d-md-flex mt-2">
        <%= f.submit "Submit but boring and normal" %>
        <%= invisible_recaptcha_tags ui: :button, text: 'Send Reset Instructions', class: 'btn btn-primary col-md-8 me-2' %>
        <%= link_to "Back To Log in", new_session_path(resource_name), class: "btn btn-secondary col" %>
      </div>
    <% end %>
  </div>
</div>

