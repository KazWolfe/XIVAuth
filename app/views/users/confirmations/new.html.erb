<div class="row justify-content-center">
  <div class="col-lg-4">
    <h2>Resend Confirmation Email</h2>
    <p>We get it. Our mailbot screws up. Fortunately, we have this easy form to help you request a new confirmation
      message.</p>

    <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }, data: { turbo: false }) do |f| %>
      <%= render "users/shared/error_messages", resource: resource %>
      <%= flash[:recaptcha_error] %>

      <div class="form-group">
        <%= f.label :email %><br/>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control",
                          value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email) %>
      </div>

      <div class="form-group d-grid d-md-flex mt-2">
        <%= invisible_recaptcha_tags ui: :button, text: 'Resend Confirmation Email', class: 'btn btn-primary col-md-8 me-2' %>
        <%= link_to "Back To Log in", new_session_path(resource_name), class: "btn btn-secondary col" %>
      </div>
    <% end %>
  </div>
</div>

