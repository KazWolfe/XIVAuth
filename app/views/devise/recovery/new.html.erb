<h2 class="mb-4">Recover Account</h2>

<p>Forgot your password or never got a confirmation email? No problem.</p>
<p>Enter your email address, and we'll kick a Moogle in your general direction to figure out what went wrong.</p>

<%= form_for(@user, as: :user, url: user_recovery_path, html: { method: :post }, data: { controller: "form--turnstile" }) do |f| %>
  <div class="form-group field">
    <%= f.label :email, class: "form-label" %><br/>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control", placeholder: eegg_random_npc_email %>
  </div>

  <div class="d-flex justify-content-center">
    <%= render partial: "layouts/components/form_elements/turnstile_preflight", locals: { action: "reset_password" } %>
  </div>
  <div class="form-group d-grid d-md-flex mt-2">
    <%= f.submit 'Send Reset Instructions', class: 'btn btn-primary col-md-8 me-2' %>
    <%= link_to "Back To Login", new_user_session_path, class: "btn btn-secondary col" %>
  </div>
<% end %>
