<% status_text = Rack::Utils::HTTP_STATUS_CODES[status] || "HTTP Error #{status}" %>
<% content_for :title, "XIVAuth — #{status}: #{status_text}" %>

<div class="text-center">
  <p class="display-1 fw-bold text-muted mb-0"><%= status %></p>
  <h1 class="h3 mb-3"><%= status_text %></h1>
  <p class="text-muted">
    XIVAuth ran into an error while handling your request.
  </p>
  <p class="text-muted">
    Please verify your request and try again. If this problem persists, please contact an XIVAuth developer.
  </p>
  <%= link_to "Back to Safety", link_to_safety, class: "btn btn-primary" %>
</div>
